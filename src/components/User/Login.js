import React from 'react';
import { Content, Text } from 'native-base';
import Container from 'SafeContainer';

export default () => (
  <Container>
    <Content>
      <Text>Login</Text>
    </Content>
  </Container>
);
